These git ignore files are crafted from the gitignore templates provided by 
github.

Git templates used in these git ignore files are found in the ./dflts/ directory

mixed_lang/ holds gitignore files that combine languages
single_lng/ holds gitignore files for individual languages
